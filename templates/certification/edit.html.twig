{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Édition #{{ certification.id }} - {{ certification.name }}{% endblock %}

{% block header %}
    <div class="row align-items-end">
        <div class="col-lg-8">
            <div class="page-header-title">
                <div class="d-inline">
                    <h4>Édition #{{ certification.id }} - {{ certification.name }}</h4>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="page-header-breadcrumb">
                <ul class="breadcrumb-title">
                    <li class="breadcrumb-item">
                        <a href="{{ path('index') }}">
                            <i class="feather icon-home"></i>
                        </a>
                    </li>
                    <li class="breadcrumb-item"><a href="{{ path('certification') }}">Liste des Certifications</a></li>
                    <li class="breadcrumb-item">Édition #{{ certification.id }} - {{ certification.name }}</li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h5>Édition #{{ certification.id }} - {{ certification.name }}</h5>
            <div class="float-right">
                <a href="{{ path('certification') }}" class="btn btn-mat btn-info"><i class="fa fa-chevron-left"></i>Retour</a>
                <button class="btn btn-mat btn-success" onclick="document.forms['certification'].submit();"><i class="fa fa-save"></i>Sauvegarder</button>
                <a data-toggle="confirm" href="{{ path('certification_remove', { id : certification.id }) }}" class="btn btn-mat btn-danger"><i class="fa fa-trash"></i>Supprimer</a>
            </div>
        </div>
        <div class="card-block">
            <div class="row">
                <div class="col-sm-12">
                    {{ include('certification/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
